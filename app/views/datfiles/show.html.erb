
<h2>DAT File</h2>
<p>
    <strong>Name:</strong>
    <%= @datfile.name %>
  </p>
   
  <p>
    <strong>Description:</strong>
    <%= @datfile.readme %>
  </p>
  <p>
      <strong>Description:</strong>
      <%= @datfile.date %>
    </p>
  <%= link_to 'Edit', edit_datfile_path(@datfile) %> | 
  <%= link_to 'Destroy', datfile_path(@datfile),
  method: :delete,
  data: { confirm: 'Are you sure?' } %></td>
   | <%= link_to 'Back', datfile_path %>
  <%= render '/welcome/bottom' %>